cmake_minimum_required(VERSION 2.8)
project( generateNoise )
find_package( OpenCV REQUIRED )
add_executable( generateNoise generateNoisyData.cc )
target_link_libraries( generateNoise ${OpenCV_LIBS} )
